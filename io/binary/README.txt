This directory shows writing a binary data using the 'struct' package
to convert an int to a string for use in write(), and a Java program
reading the same binary data file (using a DataInputStream) to confirm
byte order correctness.
